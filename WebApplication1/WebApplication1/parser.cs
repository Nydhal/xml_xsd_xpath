using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.Xml;

namespace WebApplication1
{
    public class parser
    {
        //public static string OutputNode(XmlNode node)    // recursive
        //{
        //    string result = "";

        //    if (node == null)
        //        System.Environment.Exit(1);

        //    result +="<ol>";
        //    if (node.HasChildNodes)
        //    {
        //        XmlNodeList children = node.ChildNodes;
        //        foreach (XmlNode child in children)
        //            OutputNode(child);
        //        result += node.Value+"</ol>";
        //    }

        //    return result;
        //}

    }
}